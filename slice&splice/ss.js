let colors = ['red', 'blue', 'black', 'white', 'green', 'yellow', 'purple', 'orange', 'grey', 'pink']
let slice = colors.slice(2,5)
let splice = colors.splice(1,0,'DARKBLUE')
//syntax for slice --- slice(starting posn , ending posn-1)
// syntax for splice --- splice(starting posn , number of elements to be deleted, the element to be added)